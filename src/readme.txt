MySQL Error Log for ELMAH
=========================

In order to complete the installation of the `elmah.mysql` package, you
will have to execute the `MySql.sql` script (located in the root of
the directory where the package was installed) against your MySQL database.
It will create the necessary database objects for the error log.
